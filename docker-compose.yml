version: "3.9"

services:
  ledger-server:
    build:
      context: .
      dockerfile: Dockerfile.ledger-server
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    restart: unless-stopped

