# Data directory

`data/` is the local development storage location for append-only ledger files generated by `ledger-server`.

Typical file:

- `ledger.jsonl`: newline-delimited `AuditRecord` entries written by the ingestion API.

This directory is for local/dev artifacts and fixtures, not production persistence.

